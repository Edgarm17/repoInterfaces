
#ifndef MAINWINDOW_H
#define MAINWINDOW_H

#include <QMainWindow>
//includes

class VentanaPrincipal : public QMainWindow {
Q_OBJECT
public:
        VentanaPrincipal(QWidget * parent = 0, Qt::WindowFlags flags = 0);


private:
        //atributs i mètodes
        
        
public slots:

	//slots
	

};

#endif 
